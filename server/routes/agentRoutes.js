import express from 'express';
const router = express.Router();

// Placeholder routes for agent-specific operations
router.get('/', (req, res) => {
  res.send('Agent route');
});

export default router;
